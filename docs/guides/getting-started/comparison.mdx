---
slug: comparison
title: Comparison
description: How Waku compares architecturally with other frameworks.
category: Getting Started
order: 40
---

## How to read this page

Frameworks are best compared by their durable architectural choices, not by feature checklists that change every release. All of the frameworks below are excellent at what they are designed for; the question is which model fits your project. Details reviewed in July 2026 against each framework's documentation. Always check the linked docs for the current state.

## At a glance

|                              | Waku                                                                  | [Next.js](https://nextjs.org/docs)                  | [Astro](https://docs.astro.build)                      | [React Router](https://reactrouter.com)  | [TanStack Start](https://tanstack.com/start/latest) |
| ---------------------------- | --------------------------------------------------------------------- | --------------------------------------------------- | ------------------------------------------------------ | ---------------------------------------- | --------------------------------------------------- |
| Center of the model          | React Server Component tree                                           | Integrated application platform                     | Content-first islands architecture                     | Route modules with loaders and actions   | Type-safe router                                    |
| UI model                     | React                                                                 | React                                               | Astro components + islands in any UI framework         | React                                    | React                                               |
| React Server Components      | The default architecture                                              | Default in the App Router                           | Not used (server-rendered Astro components instead)    | Experimental                             | Experimental                                        |
| Static and dynamic rendering | Declared per page, layout, and slice                                  | Per route down to per function, framework-managed   | Static-first, with on-demand routes and server islands | Per-route prerendering and SSR/SPA modes | Full-document SSR and streaming                     |
| Caching of dynamic rendering | None implicit; explicit opt-in                                        | Framework-managed caching and revalidation          | Opt-in route caching                                   | None implicit                            | None implicit                                       |
| Data fetching                | `await` in server components                                          | Server components + framework data and caching APIs | Content collections + component frontmatter            | Route loaders                            | Route loaders + server functions                    |
| Deployment                   | Adapters: Node.js, Vercel, Netlify, Cloudflare, AWS Lambda, Deno, Bun | Node.js self-hosting + platform integrations        | Adapters for many runtimes                             | Multiple runtimes                        | Multiple providers and runtimes                     |
| Framework surface            | Deliberately small                                                    | Comprehensive                                       | Focused on content sites                               | Progressive, from library to framework   | Router-centered, deeply typed                       |

Notes: React Router is compared in its framework mode. TanStack Start is powered by [TanStack Router](https://tanstack.com/router/latest).

## Next.js

Next.js optimizes for an integrated application platform: the broadest feature set in the React ecosystem, with the framework owning much of the application lifecycle, including the caching and revalidation model around data and rendering. Waku optimizes for the opposite trade: a small framework-owned surface with direct execution semantics, no implicit cache to reason about, and ecosystem libraries for the concerns the framework doesn't own. If you want an integrated platform, Next.js is a strong choice; if you prefer to keep more of those decisions in your own hands, Waku may be the better fit.

## Astro

Astro is optimized for content-first, multi-framework sites: static-first delivery, minimal browser JavaScript, and islands from any UI library. Its server islands overlap conceptually with Waku's slices. The difference is that Waku is React-native throughout: server components, client components, layouts, pages, and slices belong to one React mental model, so a site that starts mostly static can grow meaningful server-driven application behavior without changing its UI model or adding a second component format.

## React Router

React Router is built for progressive adoption (declarative, data, and framework modes) with mature loader/action conventions and a web-standards request/response model. Its React Server Components support is currently experimental. The architectural distinction: React Router begins with routing and layers framework behavior on top; Waku begins with the React server-component model and supplies the routing needed to compose it. Data flows through route loaders there, and through component composition here.

## TanStack Start

TanStack Start, powered by TanStack Router, offers end-to-end type-safe routing, server functions, and an explicit, router-centered programming model across multiple deployment targets. It shares Waku's taste for explicitness and portability. The distinction is which abstraction sits at the center: if you want an elaborate type-safe router and server-function model as the dominant abstraction, TanStack may fit you better. Waku is the stronger fit when the abstraction you want is React itself: server components as the architecture, with routing in a supporting role.

## Next

[Use Cases](/guides/use-cases) describes the application shapes Waku fits best, and the ones where another framework is the better call.
